# Netlify Configuration for Yieldra (Vite + React)
# Ensures all routes are redirected to index.html for SPA routing

[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
